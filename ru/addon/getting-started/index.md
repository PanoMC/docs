# Начало разработки аддонов

Pano — это модульная платформа, которая позволяет разработчикам расширять ее функциональность с помощью **аддонов** (фронтенд) и **плагинов** (бэкенд). Данное руководство поможет вам понять основные концепции и архитектуру экосистемы Pano.

## 🏗️ Основная архитектура

Pano не является традиционным веб-скриптом. Это автономная платформа, работающая как один JAR-файл, которая обслуживает несколько интерфейсов SvelteKit через внутренний обратный прокси.

- **Бэкенд**: Kotlin (Vert.x, Spring DI)
- **Фронтенд**: SvelteKit, Bootstrap 5, чистый JavaScript (TypeScript не используется)
- **Среда выполнения**: Bun (быстрая среда выполнения JS)

### Что такое плагин/аддон Pano?
Полное расширение обычно состоит из двух частей:
1. **Бэкенд плагина**: JAR-файл на Kotlin/Java, который работает внутри Pano Core.
2. **UI плагина (аддон)**: Фронтенд на базе Svelte, поставляемый вместе с бэкендом или обслуживаемый динамически.

---

## 🛠️ Pano SDK (`@panomc/sdk`)

SDK — это сердце разработки аддонов. Он служит мостом между хостом (Pano) и вашим плагином.

- **Провайдер компонентов**: Доступ к готовым компонентам, таким как `Button`, `Modal`, `Editor` и `Card`.
- **Регистрация UI**: Добавление новых маршрутов в панель или тему с помощью `pano.ui.page.register`.
- **Управление навигацией**: Динамическое изменение ссылок боковой панели через `pano.ui.nav.site.editNavLinks`.
- **Средства API**: Использование `ApiUtil` для безопасных сетевых запросов.
- **Локализация**: Использование локализованной функции `_` для поддержки нескольких языков (EN, TR, RU).

---

## 🎨 Стандарты фронтенда

Мы придерживаемся строгих стандартов дизайна и кодирования, чтобы все аддоны ощущались как родная часть Pano.

### Язык и стилизация
- **Чистый JavaScript**: Мы верим, что с чистым JS мир станет лучше. TypeScript не принимается во фронтенд-проектах.
- **Svelte и Bootstrap 5**: Основной UI-фреймворк. Используйте SASS для стилизации.
- **Визуальное соответствие**: Дизайн должен соответствовать эстетике `panel-ui` и `vanilla-theme`. Используйте согласованные структуры таблиц, полей поиска и пагинацию (по умолчанию 10 элементов).

### Производительность
- **SSR и CSR**: Pano использует гибридный рендеринг (на стороне сервера и на стороне клиента). Убедитесь, что ваши компоненты совместимы с SSR (избегайте обращения к `window`/`document` вне `onMount`).
- **Динамическая загрузка**: Компоненты, зарегистрированные в Pano API, **должны** загружаться динамически, чтобы минимизировать начальный вес страницы.

---

## ⚙️ Стандарты бэкенда (Kotlin)

### Управление жизненным циклом
Плагины должны расширять класс `PanoPlugin` и обрабатывать хуки `onStart` и `onUninstall`. Для операций с базой данных всегда ждите завершения настройки Pano с помощью `SetupEventListener`.

### База данных и права доступа
- **Безопасность типов**: Используйте **Enums** вместо статических строк для конфигурации и сущностей базы данных.
- **Паттерн DAO**: Используйте аннотации `@DBEntity`, `@Migration` и `@Dao`.
- **Безопасность**: Определяйте права доступа, расширяя `PanelPermission` с использованием кастомных иконок FontAwesome.
- **Логи активности**: Все административные API-эндпоинты **обязаны** определять логи активности (Activity Logs) для обеспечения прозрачности.

---

## 📥 Процесс разработки

### Быстрое тестирование UI
Чтобы протестировать изменения UI без пересборки:
1. Включите **Dev Mode** в `Панель -> Настройки`.
2. Поместите файлы вашего аддона в директорию `plugins/` вашей установки Pano.
3. Используйте команду `bun dev`, чтобы мгновенно видеть изменения благодаря Hot Module Replacement (HMR).

### Ветки и коммиты
- **Сначала Alpha**: Всегда ориентируйтесь на ветку `alpha` для Pano Core или ветку `dev` для UI-проектов.
- **Conventional Commits**: Мы строго следуем стандарту [Conventional Commits](https://www.conventionalcommits.org/). Это обязательно, так как ченджлоги генерируются автоматически через **Semantic Release**.

---

## 🔗 Ресурсы и лицензирование
- **Лицензия**: Официальные плагины и шаблоны распространяются под лицензией **MIT**.
- **Сообщество**: Присоединяйтесь к нам в [Discord](https://panomc.com/discord) для поддержки.
- **Шаблон (Boilerplate)**: Начните свой проект с [Pano Boilerplate Plugin](https://github.com/PanoMC/pano-boilerplate-plugin).

Готовы к созданию? Переходите к следующему разделу, чтобы настроить среду разработки!
